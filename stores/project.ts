import { defineStore } from 'pinia'

export const useProjectStore = defineStore('project', {
  state: () => ({
    // TODO: define Project store state
  }),
  actions: {
    // TODO: define Project actions
  }
})
